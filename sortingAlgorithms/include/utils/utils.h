#ifndef UTILS_H
#define UTILS_H

namespace UTILS {
    void swap(int& A, int& B);
    void printArr(int* arr, int N);
    void resetArr(int* arr, int N);
    int arrSize(int* arr);
};

#endif