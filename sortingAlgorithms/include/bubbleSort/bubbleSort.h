#ifndef BUBBLESORT_H
#define BUBBLESORT_H

namespace BUBBLE{
    // Utility

    void swap(int& A, int& B);
    void printArray(int* arr, int N);
    void bubbleSort(int* arr, int N);
};

#endif